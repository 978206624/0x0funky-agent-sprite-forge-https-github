// 右栏对话停靠面板实现在 components/right/right-tabs.tsx（仅对话，可拖拽改宽/可收起）。
// 参数面板已迁至 components/center/params-panel.tsx（中间区左列）。
// 保留本文件作为 layout 层稳定导入路径（App.tsx 仍 import './components/layout/right-panel'）。
export { RightPanel } from '../right/right-tabs'
